<h1>ジャンル編集</h1>

<%= form_with model: @genre do |f| %>

  <div class = "field">
    <a>ジャンル名</a>
     <%= f.string_field :name %>
    <a><%= f.submit '変更を保存', class:"btn btn-success" %></a>
  </div>

<% end %>
