<h1>ジャンル編集</h1>

<%= form_with model: @genre, url: admin_genre_path(@genre.id) do |f| %>

  <div class = "field">
    <a>ジャンル名</a>
     <%= f.text_field :name %>
    <a><%= f.submit '変更を保存', class:"btn btn-success" %></a>
  </div>

<% end %>
